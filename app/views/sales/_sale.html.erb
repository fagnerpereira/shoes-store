<tr id="<%= dom_id sale %>">
  <td>
    <%= sale.store.name %>
  </td>
  <td>
    <%= sale.product.name %>
  </td>
  <td>
    <%= number_to_currency(sale.price) %>
  </td>
  <td>
    <%= sale.created_at.strftime('%y-%m-%d %H:%M') %>
  </td>
</tr>
